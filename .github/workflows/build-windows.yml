name: Build Windows Installer

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install dependencies
      working-directory: electron-app
      run: |
        # Remove package-lock for fresh install
        if (Test-Path package-lock.json) {
          Remove-Item package-lock.json
        }

        # Install dependencies
        npm install

    - name: Prepare Whisper assets
      working-directory: electron-app
      run: npm run prepare-whisper

    - name: Prepare ffmpeg assets
      working-directory: electron-app
      run: npm run prepare-ffmpeg

    - name: Build Windows installer
      working-directory: electron-app
      env:
        CSC_IDENTITY_AUTO_DISCOVERY: false
      run: |
        # Build with electron-builder (installer)
        npx electron-builder --win --publish never

    - name: Upload installer
      uses: actions/upload-artifact@v4
      with:
        name: StreamVoice-Setup
        path: electron-app/dist/*.exe

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: electron-app/dist/StreamVoice-Setup-*.exe
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
